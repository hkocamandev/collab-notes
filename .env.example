# Copy to .env (which is gitignored) before running `docker compose up`.
#
# JWT secret used to sign auth tokens. Must be at least 32 chars — the env
# loader (server/src/env.ts) refuses to start otherwise.
# Generate with:  openssl rand -base64 48
JWT_SECRET=replace-me-with-a-random-48-byte-base64-string
